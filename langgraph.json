{
    "dependencies": ["."],
    "graphs":{
        "Failing Test Case Generator": "./graph/graph.py:app"
    },
    "env": ".env",
    "dockerfile_lines": [
        "RUN apt-get update && apt-get install -y gcc"
    ]
}